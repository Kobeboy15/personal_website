// Shared intro timing. The preloader's curtain begins lifting at ~1.85s, so the
// hero reveal is delayed to start in sync — they read as one continuous motion.
// Keep this in step with the Preloader timeline if its durations change.
export const HERO_REVEAL_DELAY = 1.9;
